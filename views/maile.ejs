<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <title>Lista Maili</title>
    <style>
        body { font-family: sans-serif; padding: 20px; background-color: #f8f9fa; display: flex; flex-direction: column; align-items: center; }
        h1 { color: #333; margin-bottom: 20px; }

        /* Panel Sterowania */
        .control-panel { background: white; padding: 20px; border-radius: 8px; margin-bottom: 20px; box-shadow: 0 2px 5px rgba(0,0,0,0.1); text-align: center; width: 100%; max-width: 800px; }
        .btn { padding: 10px 20px; border: none; border-radius: 4px; cursor: pointer; color: white; font-weight: bold; margin: 5px; }
        .btn-start { background-color: #28a745; }
        .btn-stop { background-color: #dc3545; }
        .btn-sim { background-color: #ffc107; color: #333; }

        /* Tabela */
        table { border-collapse: collapse; width: 100%; max-width: 1200px; background: white; box-shadow: 0 2px 5px rgba(0,0,0,0.1); }
        th, td { padding: 15px; text-align: left; border-bottom: 1px solid #ddd; }
        th { background-color: #343a40; color: white; }
        
        /* Interakcja wierszy */
        tr.mail-row { cursor: pointer; transition: background 0.2s; }
        tr.mail-row:hover { background-color: #e9ecef; }

        /* Statusy - kolorki */
        .badge { padding: 5px 10px; border-radius: 12px; font-size: 12px; color: white; font-weight: bold; text-transform: uppercase; display: inline-block; min-width: 80px; text-align: center; }
        .status-oczekuje { background-color: #007bff; } /* Niebieski */
        .status-w-trakcie { background-color: #6c757d; } /* Szary */
        .status-ok { background-color: #28a745; }       /* Zielony */
        .status-error { background-color: #dc3545; }    /* Czerwony */

        .back-link { margin-top: 20px; color: #666; text-decoration: none; }
    </style>
</head>
<body>

    <h1>Lista Zadań (Maile)</h1>

    <div class="control-panel">
        <h3>Status usługi: 
            <%= statusPobierania ? '🟢 AKTYWNA' : '🔴 ZATRZYMANA' %>
        </h3>
        <form action="/zaczytywanie/maile/sterowanie" method="POST" style="display:inline;">
            <input type="hidden" name="akcja" value="start">
            <button type="submit" class="btn btn-start" <%= statusPobierania ? 'disabled' : '' %>>▶ WŁĄCZ</button>
        </form>
        <form action="/zaczytywanie/maile/sterowanie" method="POST" style="display:inline;">
            <input type="hidden" name="akcja" value="stop">
            <button type="submit" class="btn btn-stop" <%= !statusPobierania ? 'disabled' : '' %>>⏹ STOP</button>
        </form>
        <form action="/zaczytywanie/maile/dodaj-testowy" method="POST" style="display:inline; margin-left: 20px;">
            <button type="submit" class="btn btn-sim">+ Symuluj Mail</button>
        </form>
    </div>

    <table>
        <thead>
            <tr>
                <th style="width: 15%">Data</th>
                <th style="width: 20%">Adresat</th>
                <th style="width: 45%">Temat</th>
                <th style="width: 20%">Status</th>
            </tr>
        </thead>
        <tbody>
            <% maile.forEach(function(mail) { %>
                <tr class="mail-row" onclick="window.location='/zaczytywanie/maile/szczegoly/<%= mail._id %>'">
                    <td><%= mail.dataOdebrania %></td>
                    <td><%= mail.adresat %></td>
                    <td style="font-weight: bold;"><%= mail.temat %></td>
                    <td>
                        <% 
                            let sClass = 'status-oczekuje';
                            if(mail.status === 'w trakcie') sClass = 'status-w-trakcie';
                            if(mail.status === 'ok') sClass = 'status-ok';
                            if(mail.status === 'error') sClass = 'status-error';
                        %>
                        <span class="badge <%= sClass %>"><%= mail.status %></span>
                    </td>
                </tr>
            <% }); %>
            <% if (maile.length === 0) { %>
                <tr><td colspan="4" style="text-align:center; padding: 20px;">Brak maili.</td></tr>
            <% } %>
        </tbody>
    </table>

    <a href="/zaczytywanie" class="back-link">← Wróć do menu</a>

</body>
</html>